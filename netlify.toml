[build]
  publish = "."
  command = ""

[functions]
  node_bundler = "esbuild"

[dev]
  framework = "#static"
  publish = "."
  port = 8888

[[headers]]
  for = "/run/*.json"
  [headers.values]
    Cache-Control = "public, max-age=300"
